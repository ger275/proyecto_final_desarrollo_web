@model ProyectoFinalDesarrolloWeb.Models.PacienteHabitacionModel

@{
    ViewData["Title"] = "Editar";
}

<h1>Editar</h1>

<h4>Paciente en Habitación</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Editar">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
                <input asp-for="Id" class="form-control" readonly />
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="idPaciente" class="control-label"></label>
                @*<input asp-for="idPaciente" class="form-control" />*@
                <select asp-for="idPaciente" class="form-select" asp-items="@(new SelectList(ViewBag.Pacientes, "Value", "Text", ""))"></select>
                <span asp-validation-for="idPaciente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="idHabitacion" class="control-label"></label>
                @*<input asp-for="idHabitacion" class="form-control" />*@
                <select asp-for="idHabitacion" class="form-select" asp-items="@(new SelectList(ViewBag.Habitaciones, "Value", "Text", ""))"></select>
                <span asp-validation-for="idHabitacion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="fechaHoraEntrada" class="control-label"></label>
                <input asp-for="fechaHoraEntrada" class="form-control" />
                <span asp-validation-for="fechaHoraEntrada" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="fechaHoraSalida" class="control-label"></label>
                <input asp-for="fechaHoraSalida" class="form-control" />
                <span asp-validation-for="fechaHoraSalida" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group row g-2">
                <div class="col-auto">
                    <input type="submit" value="Guardar" class="btn btn-primary" />
                </div>
                <div class="col-auto">
                    <a asp-action="Index" class="btn btn-secondary">Regresar</a>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
